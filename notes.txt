commands:
    files/dirs:
        - mkdir, touch, rm
    open/view:
        - open, cat, view, edit
    search:
        - find, search
    utils:
        - help, etc.

architecture:
    - move the corresponding functions to different files

features: 
    - live file preview panel (text, images, archives) with auto refresh on selection
    - fuzzy search
    - marks/bookmarks/quick/jumps + nav history
    - bulk rename tool
    - trash + undo
    - background operations
    - git integration
    - plugin/hooks system
    - dual-pane/tabs/splits

